title: Venue

status:
  draft: true
  listed: true

options:
  changeSlug: false
  delete: false
  preview: false

columns:
  - width: 1/2
    fields:
      address:
        label: Address
        type: text
      hours:
        label: Opening Hours
        type: text
        placeholder: "Wednesday to Sunday, 12:00–19:00"
      image:
        label: Venue Image
        type: files
        max: 1
        layout: cards
  - width: 1/2
    fields:
      blocks:
        extends: fields/blocks
